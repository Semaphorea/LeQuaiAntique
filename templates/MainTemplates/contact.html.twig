{% extends 'base.html.twig' %}

  {% block Contact %}
   
     
          {{form_start(contact, {'attr': {'class': 'd-grid  justify-content-center '}} ) }}
          {{form_rest(contact)}}
          {{form_end(contact)}}   
        
     

  
  
   
  {% endblock %} 