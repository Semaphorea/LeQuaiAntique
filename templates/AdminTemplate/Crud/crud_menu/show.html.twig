{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
 {% block FormPage %}
  <div id="container" class='crud-container d-grid justify-content-center' >
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ menu.Titre }}</td>
            </tr>
            <tr>
                <th>Formule</th>
                <td>{{ menu.Formule ? menu.Formule|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_menus_formules') }}">Back to Gestion Menu</a>

    <a href="{{ path('app_crud_menu_edit', {'id': menu.id}) }}">edit</a>

    {{ include('/AdminTemplate/Crud/crud_menu/_delete_form.html.twig') }}
    </div>
{% endblock %}
</div>
{% endblock %}
